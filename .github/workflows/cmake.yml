jobs:
  build:
    runs-on: '${{ matrix.os }}'
    steps:
      - uses: 'actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1'
        with:
          persist-credentials: false
      - if: "runner.os == 'Linux'"
        name: 'Install dependencies (Linux)'
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev
      - if: "runner.os == 'macOS'"
        name: 'Install dependencies (macOS)'
        run: 'brew install openssl@3'
      - name: 'Configure'
        run: >-
          cmake -B ${{ github.workspace }}/build
          -DCMAKE_BUILD_TYPE=${{ matrix.build-type }}
      - name: 'Build'
        run: >-
          cmake --build ${{ github.workspace }}/build
          --config ${{ matrix.build-type }} --verbose
      - name: 'Set up Python'
        uses: 'actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97'
        with:
          python-version: '3.x'
      - name: 'Install Python build tooling'
        run: 'python -m pip install --upgrade build'
      - name: 'Build Python wheel'
        run: 'python -m build --wheel'
      - name: 'Stage artifacts'
        shell: 'bash'
        run: |
          set -e
          mkdir -p stage
          cp dist/*.whl stage/
          bin=$(find "${{ github.workspace }}/build/src" -maxdepth 2 -name 'dbeaver-creds' -o -name 'dbeaver-creds.exe' | head -1)
          if [ -n "$bin" ]; then cp "$bin" stage/; fi
      - name: 'Upload workflow artifacts'
        uses: 'actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a'
        with:
          if-no-files-found: 'error'
          name: 'dbeaver-creds-${{ matrix.os }}-${{ matrix.build-type }}'
          path: 'stage/*'
      - if: "matrix.build-type == 'Release' && github.ref_type == 'tag' && runner.os == 'Windows'"
        name: 'Install NSIS'
        shell: 'pwsh'
        run: 'choco install nsis -y --no-progress'
      - if: "matrix.build-type == 'Release' && github.ref_type == 'tag' && runner.os != 'Linux'"
        name: 'Generate release packages via CPack'
        shell: 'bash'
        run: |
          set -e
          mkdir -p '${{ github.workspace }}/release-stage'
          (cd '${{ github.workspace }}/build' && \
            cpack -C '${{ matrix.build-type }}' \
                  -B '${{ github.workspace }}/release-stage')
      - env:
          GH_TOKEN: '${{ github.token }}'
          TAG: '${{ github.ref_name }}'
        if: "matrix.build-type == 'Release' && github.ref_type == 'tag' && runner.os != 'Linux'"
        name: 'Attach Release artifacts to draft GitHub release'
        run: |
          set -e
          shopt -s failglob
          files=(release-stage/dbeaver-creds-*)
          gh release view "${TAG}" >/dev/null 2>&1 ||
            gh release create "${TAG}" --draft --title "${TAG}" --notes '' || true
          gh release upload "${TAG}" "${files[@]}" --clobber
        shell: 'bash'
    strategy:
      fail-fast: false
      matrix:
        build-type:
          - 'Debug'
          - 'Release'
        os:
          - 'macos-15-intel'
          - 'macos-latest'
          - 'ubuntu-24.04-arm'
          - 'ubuntu-latest'
          - 'windows-11-arm'
          - 'windows-latest'
name: 'Build'
'on':
  pull_request:
    branches:
      - 'master'
    paths:
      - '**/*.c'
      - '**/*.h'
      - '**/*.py'
      - '**/CMakeLists.txt'
      - '.github/workflows/cmake.yml'
      - 'pyproject.toml'
  push:
    branches:
      - 'master'
    paths:
      - '**/*.c'
      - '**/*.h'
      - '**/*.py'
      - '**/CMakeLists.txt'
      - '.github/workflows/cmake.yml'
      - 'pyproject.toml'
    tags:
      - 'v*'
permissions:
  contents: 'write'
